httpPort = 4207

db.user = null
db.password = null
db.url = null

useFileSystemAssets = false

useSecureCookie = true
cookieSameSite = "Lax"
cookieEncryptionKey = ""
cookieSigningKey = ""

// CORS Configuration
// Hosts that are allowed for cross-origin requests (HTTP/HTTPS based on environment)
cors.allowedHosts = []

// Hosts that are allowed ONLY via HTTPS (enforced in production)
cors.allowedHttpsHosts = []
