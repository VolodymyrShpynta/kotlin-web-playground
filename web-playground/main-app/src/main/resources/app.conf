httpPort = 4207

db.user = null
db.password = null
db.url = null

useFileSystemAssets = false

useSecureCookie = true
cookieSameSite = "Lax"
cookieEncryptionKey = ""
cookieSigningKey = ""

// CORS Configuration
// Hosts that are allowed for cross-origin requests (HTTP/HTTPS based on environment)
cors.allowedHosts = []

// Hosts that are allowed ONLY via HTTPS (enforced in production)
cors.allowedHttpsHosts = []

// Third-party service URL for async HTTP call demonstrations
// Override this in environment-specific configs (app-local.conf, app-prod.conf, etc.)
thirdPartyServiceUrl = ""

